DISCORD_TOKEN=your_bot_token_here
DATABASE_PATH=data/bot.db                          # SQLite file. On Railway/Docker, mount a volume to persist.
ADMIN_CHANNEL_ID=                                   # Discord channel ID for match-started admin log (screenshots cross-ref)
DEV_GUILD_ID=                                       # optional; if set, syncs commands only to this guild
